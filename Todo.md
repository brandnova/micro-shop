I undid all Leapcell storage integration. It is giving too many issues that I don't want to handle right now. I reverted to the Cloudinary integration, but I'll be leaving the variable empty so it falls back to local storage. I will look into the integration later since I am not too familiar with cloudinary setup yet.

For the database, I left the integration. I'll also be using the fallback SQLite, but the settings should be there.

